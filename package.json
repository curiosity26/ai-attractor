{
  "name": "@curiosity26/ai-attractor",
  "version": "0.1.0",
  "description": "DOT-based pipeline runner for multi-stage AI workflows",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "type": "module",
  "bin": {
    "ai-attractor": "./dist/cli.js"
  },
  "scripts": {
    "run": "tsx cli.ts run",
    "validate": "tsx cli.ts validate",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.1"
  }
}
